# @Name: Default
# @List: aptList
aptList=(
    # Core
    "build-essential"
    "autoconf"
    "libssl-dev"
    "pkg-config::pkg-config is a system for managing library compile and link flags that works with automake and autoconf"
    "systemd::System and service manager https://wiki.debian.org/systemd"
    
    # Development
    "docker-ce::Docker provides a way to run applications securely isolated in a container, packaged with all its dependencies and libraries https://docs.docker.com/"

    # Security
	"rkhunter::Rootkit detection library"
	"chkrootkit::Yet another rootkite detection library"
    "nmap::Nmap is a free and open source network discovery and security auditing utility"
)

